#include "main.h"

/**
 * mul - test function
 * @a: first multiple
 * @b: second multiple
 * Return: the multiple of the two
 */

int mul(int a, int b)
{
	int answer;

	answer = a * b;
	return (answer);
}
